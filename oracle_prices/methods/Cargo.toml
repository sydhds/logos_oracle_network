[package]
name = "oracle_prices-methods"
version = "0.1.0"
edition = "2021"

[build-dependencies]
risc0-build = "=3.0.5"

[dependencies]
risc0-zkvm = { version = "=3.0.5", features = ["std"] }
oracle_prices_core = { path = "../oracle_prices_core" }
