[package]
name = "oracle_register_core"
version = "0.1.0"
edition = "2021"

[dependencies]
serde = { version = "1.0", features = ["derive"] }
borsh = { version = "1.5", features = ["derive"] }
tiny-keccak = { version = "2.0.2", features = ["keccak"] }

[dev-dependencies]
rs_merkle = "1.4"